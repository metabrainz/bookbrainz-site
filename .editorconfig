root = true

# Unix-style newlines with a newline ending every file
# Somewhat redundant as git already handles this
[*]
charset = utf-8
end_of_line = lf
indent_size = 4
indent_style = tab
insert_final_newline = true

# The YAML spec explicitly forbids tabs
[*.yml]
indent_style = space
