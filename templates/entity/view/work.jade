extends ./entity

block icon
	span.fa.fa-file-text-o

block picture
	span.fa.fa-file-text-o.picture-fallback-icon-large

block attributes
	+type(entity.workType)

	dt Languages
	dd
		if entity.languageSet && entity.languageSet.languages
			=entity.languageSet.languages.map(function(l){return l.name;}).join(', ')
		else
			| ?
